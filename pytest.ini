[pytest]
pythonpath = api-core/src
testpaths = api-core/tests
markers =
    integration: tests that spawn the Java event-service over the subprocess bridge (require a built event-service; deselect with -m 'not integration')