spring.datasource.url=jdbc:h2:mem:yse;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.username=sa
spring.datasource.password=
# No entities/Flyway yet; create-drop keeps tests self-contained until #90
# introduces migrations, at which point this becomes validate.
spring.jpa.hibernate.ddl-auto=create-drop